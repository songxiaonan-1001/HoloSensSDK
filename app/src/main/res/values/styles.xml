<resources>

    <!-- Base application theme. -->


    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/window</item>
    </style>


    <style name="UpdateDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="FingerDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--自定义耗时对话框-->


    <!-- 自定义CustomDialog中的样式(报警Dialog用) -->
    <style name="customDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="InputLayout" parent="Widget.Design.TextInputLayout">
        <!-- 默认状态提示颜色和字体-->
        <item name="android:textColorHint">@color/black_50</item>
        <item name="android:textSize">@dimen/text_size_15</item>
        <item name="android:textColor">@color/local_dev_text_color</item>
        <!-- 下面可以根据需要设置，如果没有将默认使用默认theme的颜色 -->
        <!-- Label标签的颜色 -->
        <item name="colorAccent">@color/main</item>
        <!-- 默认状态状态设置底线颜色 -->
        <item name="colorControlNormal">@color/edit_text_hint</item>
        <!-- 选择的底线，hint字体颜色 -->
        <item name="colorControlActivated">@color/main</item>
    </style>

    <style name="HintTextAppearance" parent="Widget.Design.TextInputLayout">
        <item name="android:textSize">11sp</item>
        <item name="android:textColor">@color/edit_text_hint_new</item>
    </style>

    <style name="ErrorTextAppearance" parent="Widget.Design.TextInputLayout">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/edit_text_error</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">44dp</item>
        <item name="android:layout_marginTop">@dimen/margin_30</item>
        <item name="android:background">@drawable/selector_btn_login</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/btn_login_text_color</item>
    </style>

    <!-- For GifView Declaration -->

    <declare-styleable name ="CustomEditTextView">
    <attr name ="edittext_max_number" format ="integer"/>
    <attr name ="edittext_hint_text" format ="string"/>
    <attr name ="edittext_color" format ="color"/>
    <attr name ="edittext_size" format ="dimension"/>
    <attr name ="edittext_hint_color" format ="color"/>
    <attr name ="lines_color" format ="color"/>
    <attr name ="textview_text_color" format ="color"/>
    <attr name ="textview_text_size" format ="dimension"/>

</declare-styleable>
    <style name="AVLoadingIndicatorView">
        <item name="minWidth">96dp</item>
        <item name="maxWidth">96dp</item>
        <item name="minHeight">60dp</item>
        <item name="maxHeight">60dp</item>
        <item name="indicatorName">BallPulseIndicator</item>
    </style>

    <style name="ActionSheetStyle" parent="@android:style/Theme.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetAnimation</item>
    </style>

    <!-- ActionSheet进出动画 -->
    <style name="ActionSheetAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/actionsheet_in</item>
        <item name="android:windowExitAnimation">@anim/actionsheet_out</item>
    </style>

    <!-- PopupWindow进出动画 -->

    <!--    设备编辑textview style-->

</resources>
